import styled from 'styled-components'
import { cores } from '../../styles'

const CabecalhoEstilo = styled.div`
  text-align: center;
  background-color: ${cores.secundaria};
  color: ${cores.primaria};
  padding: 24px 0;
`
export default CabecalhoEstilo
